query HentKontaktadresseQuery($ident: ID!) {
    person: hentPerson(ident: $ident) {
        kontaktadresse {
            metadata {
                master
                opplysningsId
            }
        }
    }
}
